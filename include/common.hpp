#ifndef COMMON_HPP
#define COMMON_HPP

// Common static constant definitions

// NOTE: MUST be divisible by 2 due to bitsize constraints
const int BUFFER_SIZE = 4096;
const int MIN_BIT_SIZE = 12;
const int MAX_BIT_SIZE = 16;

#endif /* COMMON_HPP */
